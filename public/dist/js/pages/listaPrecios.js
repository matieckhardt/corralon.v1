const elem = document.getElementById('grid');
const msnry = new Masonry( elem, {
  // options
  itemSelector: '.grid-item',
  columnWidth: 500
});
const elem2 = document.getElementById('grid2');
const msnry2 = new Masonry( elem2, {
  // options
  itemSelector: '.grid-item',
  columnWidth: 500
});


